type: u32
default: 3544
help: IPv4 port of the teredo relay, must be publicly accessible.
val_help: u32:1-65535; Numeric IP port

syntax:expression: $VAR(@) > 0 && $VAR(@) <= 65535 ; \
	"Port number bust be in range 1 to 65535"
